export default {

  add(params) {
    return params
  },

  remove(id) {
    return id
  },

  appendToList(list, name) {
    return { list, name}
  },

  removeListItem(item) {
    return { item }
  }

}
